"use client";

import EditorContainer from "@/lib/client/editor/EditorContainer";

export default function QuestionPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between">
      <EditorContainer />
    </main>
  );
}
